<?xml version="1.0" encoding="utf-8"?>
<manifest xmlns:android="http://schemas.android.com/apk/res/android"
    package="com.project.anonymousshrink">

    <uses-permission android:name="android.permission.INTERNET" />

    <application
        android:allowBackup="true"
        android:icon="@mipmap/ic_launcher"
        android:label="@string/app_name"
        android:roundIcon="@mipmap/ic_launcher_round"
        android:supportsRtl="true"
        android:theme="@style/AppTheme">
        <activity
            android:name=".Activities.SplashScreen"
            android:theme="@style/AppTheme.NoActionBar">
            <intent-filter>
                <action android:name="android.intent.action.MAIN" />

                <category android:name="android.intent.category.LAUNCHER" />
            </intent-filter>
        </activity>
        <activity
            android:name=".Activities.Login"
            android:label="Login" />
        <activity
            android:name=".Activities.Register"
            android:label="Register" />
        <activity
            android:name=".Activities.Forgot"
            android:label="Reset Password" />
        <activity android:name=".Activities.Welcome" />
        <activity
            android:name=".Activities.DashboardRegular"
            android:label="@string/title_activity_dashboard"
            android:theme="@style/AppTheme.NoActionBar" />
        <activity android:name=".Activities.Profile" />
        <activity android:name=".Activities.DashboardGuest"
            android:label="Guest Access"/>
        <activity android:name=".Activities.AnswerQuestion"
            android:label="Answers/Comments"/>
        <activity android:name=".Activities.ProfileView"></activity>
    </application>

</manifest>